# Owner's inbox and agent tooling — never tracked (root CLAUDE.md)
UV/
.claude/
prompt.txt

# Written for the owner, in his language, to test a build by hand. Not a
# project doc and not part of the navigation chain (he asked for it ignored).
PROVERA.md

# Python guard-test caches
__pycache__/
.pytest_cache/

# Android / Gradle build output
.gradle/
.kotlin/
build/
app/build/
local.properties
.idea/
captures/

# Signing material — never in git
*.keystore
*.jks
keystore.properties

# Runtime logs
logs/
